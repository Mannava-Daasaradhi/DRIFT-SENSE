# DRIFT-SENSE — pip freeze from the development virtual environment.
# Python 3.12 (see README; the system default 3.14 has no torch wheels).
#
#   python -m pip install -r requirements.txt
#
# NOTE ON torch: it is listed because the repository ships a trained re-ranker
# and its training script, but `localize.py` DOES NOT NEED IT. The localizer
# runs the full classical pipeline with torch absent and is validated that way
# (tools/test_robustness.py). If the torch line gives you trouble, delete it —
# nothing required to reproduce the reported results will break.
#
# The CPU build is pinned deliberately: the re-ranker is ~200k parameters and
# trains in under a minute on CPU, so a 2.5 GB CUDA wheel buys nothing and the
# CPU wheel installs on a grading machine with no GPU.
#
# The extra index below is what makes the `+cpu` build resolvable; without it
# pip cannot find that local version on PyPI and the whole install fails.
--extra-index-url https://download.pytorch.org/whl/cpu

ImageIO==2.37.4
Jinja2==3.1.6
MarkupSafe==3.0.3
colorama==0.4.6
contourpy==1.3.3
cycler==0.12.1
filelock==3.29.0
fonttools==4.63.0
fsspec==2026.4.0
kiwisolver==1.5.0
lazy-loader==0.5
matplotlib==3.11.1
mpmath==1.3.0
networkx==3.6.1
numpy==2.5.1
opencv-python==5.0.0.93
packaging==26.3
pillow==12.3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
scikit-image==0.26.0
scipy==1.18.0
six==1.17.0
sympy==1.14.0
tifffile==2026.7.31
torch==2.13.0+cpu
tqdm==4.70.0
typing_extensions==4.15.0
