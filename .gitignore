.venv/
__pycache__/
*.pyc

# Local agent-tooling state (session logs, edit history) - not part of the
# submission and leaks local absolute paths; never belonged in a public repo.
.claude-flow/
.claude/

# Generated pairs are large and reproducible from a seed - keep one sample
# pair committed per repo requirement #2, gitignore the rest (PLAN.md S3).
data/*
!data/dev_v0
data/dev_v0/*
!data/dev_v0/dram_00000
!data/eval
data/eval/*
!data/eval/dram_00000

weights/*.pt
!weights/reranker.pt

# Most figures are regenerated by evaluate.py/viz.py, but the slide-deck assets
# are committed so a reviewer sees the results without running anything.
figures/*
!figures/sem_physics_stages.png
!figures/noise_ladder_accuracy.png
!figures/accuracy_vs_baseline.png
!figures/error_cdf.png
!figures/reliability.png
!figures/success_case.png
!figures/honest_failure_case.png
# Same set, run against the held-out OOD config -- the more honest comparison
# against the baseline, since the in-distribution draw happens to sit near the
# baseline's built-in assumptions (m~=10, rotation~=0).
!figures/accuracy_vs_baseline_ood.png
!figures/error_cdf_ood.png
!figures/reliability_ood.png
!figures/honest_failure_case_ood.png
!figures/fft_lattice.png
!figures/decomposition.png
!figures/pipeline_diagram.png

# Evaluation output is committed as evidence: every number quoted in the README
# and on the slides must be traceable to a file in the repository.
!results.json
!results_ood.json

# Component 1 (the PPT) is submitted separately via the i4C portal, not one of
# the 7 mandatory repo items - keep the generated deck out of the repo itself.
docs/*.pptx
docs/*.pdf
